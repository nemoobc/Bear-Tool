{
  "name": "bear-tool",
  "version": "1.0.0",
  "description": "Bear Tool — self-custody cartoon crypto wallet. All EVM networks, EIP-7702 suite. 100% client-side.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check js/app.js && node --check js/network.js && node --check js/theme.js && node --check js/ui.js && node --check js/wallet.js",
    "verify": "npm run check && npm test"
  },
  "devDependencies": {
    "ethers": "^6.13.4"
  }
}